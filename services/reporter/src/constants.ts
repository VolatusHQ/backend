/** The journal `service` name every action/cursor in this package is filed under. */
export const SERVICE = "reporter";

/** Name of the `kv` cursor tracking how far the vault `EpochOpened` backfill has scanned. */
export const EPOCH_SCAN_CURSOR = "vault-epoch-scan";
